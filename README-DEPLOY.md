# Vercel Deployment

1. Sign in to Vercel and import the GitHub repository.
2. Add environment variables (see DEPLOYMENT.md).
3. Set build command to `pnpm build` and leave output settings default for Next.js.
4. Deploy and test `/api/health`.
